begin
  require 'jeweler'
  Jeweler::Tasks.new do |s|
    s.name        = "microdata_fu"
    s.description = "Read-out (rails generated) microdata from javascript"
    s.summary     = "Passing microdata from rails to javascript"
    s.email       = "wolfger@spearwolf.de"
    s.homepage    = "http://github.com/spearwolf/microdata_fu"
    s.authors     = ["Wolfger Schramm"]
    s.files       = FileList["MIT-LICENSE", "README.rdoc", "lib/**/*", "rails/**/*"]
    s.has_rdoc    = true
    s.extra_rdoc_files = ["README.rdoc"]
  end
rescue LoadError
  puts "Jeweler not available. Install it with: sudo gem install technicalpickles-jeweler -s http://gems.github.com"
end
